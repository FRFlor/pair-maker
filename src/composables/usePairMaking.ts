export default function usePairMaking() {
    const names: string[] = [];

    function addNewNameToList(name: string) {
        return;
    }

    return {
        names,
        addNewNameToList
    }
}
